{
  "name": "value-trace",
  "version": "0.1.28",
  "private": true,
  "description": "Click a number to see which API it comes from.",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && node scripts/build.mjs",
    "icons": "node scripts/make-icons.mjs",
    "screenshots": "node scripts/capture-screenshots.mjs",
    "pack": "tsc --noEmit && node scripts/pack.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.11",
    "typescript": "^5.9.2",
    "vite": "^7.1.5",
    "vitest": "^3.2.4"
  }
}
