{
  "name": "value-trace",
  "version": "0.1.0",
  "private": true,
  "description": "Start Inspect, click a number, see its API.",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && node scripts/build.mjs",
    "test": "vitest run",
    "test-page": "node scripts/test-server.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.11",
    "typescript": "^5.9.2",
    "vite": "^7.1.5",
    "vitest": "^3.2.4"
  }
}
