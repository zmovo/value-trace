{
  "name": "value-trace",
  "version": "0.1.25",
  "private": true,
  "description": "See which API a number comes from. Click a number to start.",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && node scripts/build.mjs",
    "icons": "node scripts/make-icons.mjs",
    "pack": "tsc --noEmit && node scripts/pack.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.11",
    "typescript": "^5.9.2",
    "vite": "^7.1.5",
    "vitest": "^3.2.4"
  }
}
